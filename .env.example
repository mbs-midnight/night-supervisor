# Optional endpoint overrides (defaults shown). Copy to .env.local if needed.
# The supervised wallet's seed is NOT configured here: it is entered in the app
# at runtime and held in memory for the session only.
# VITE_NETWORK_ID=stagenet
# VITE_INDEXER_HTTP=https://indexer.stagenet.shielded.tools/api/v4/graphql
# VITE_INDEXER_WS=wss://indexer.stagenet.shielded.tools/api/v4/graphql/ws
